// Development environment configuration
// This is used when running `ng serve` (development mode)

export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api'  // Your Spring Boot backend URL
};
